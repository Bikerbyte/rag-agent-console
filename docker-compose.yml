services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cpbl-telegram-assistant
    depends_on:
      db:
        condition: service_healthy
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: Host=db;Port=5432;Database=${POSTGRES_DB};Username=${POSTGRES_USER};Password=${POSTGRES_PASSWORD}
      TelegramBot__Enabled: ${TELEGRAM_BOT_ENABLED:-false}
      TelegramBot__BotToken: ${TELEGRAM_BOT_TOKEN:-}
      TelegramBot__ApiBaseUrl: https://api.telegram.org
      TelegramBot__PollingDelaySeconds: 3
      TelegramBot__UseWebhookMode: ${TELEGRAM_BOT_USE_WEBHOOK_MODE:-false}
      TelegramBot__WebhookPath: ${TELEGRAM_BOT_WEBHOOK_PATH:-/api/telegram/webhook}
      TelegramBot__WebhookUrl: ${TELEGRAM_BOT_WEBHOOK_URL:-}
      TelegramBot__WebhookSecretToken: ${TELEGRAM_BOT_WEBHOOK_SECRET_TOKEN:-}
      AppRuntime__Profile: ${APP_RUNTIME_PROFILE:-Standard}
      AppRuntime__InstanceName: ${APP_RUNTIME_INSTANCE_NAME:-auto}
      AppRuntime__EnableLeadershipLease: ${APP_RUNTIME_ENABLE_LEADERSHIP_LEASE:-true}
      AppRuntime__LeaseDurationSeconds: ${APP_RUNTIME_LEASE_DURATION_SECONDS:-45}
      AppRuntime__LeaseRenewIntervalSeconds: ${APP_RUNTIME_LEASE_RENEW_INTERVAL_SECONDS:-15}
      AppRuntime__LeaseAcquireRetrySeconds: ${APP_RUNTIME_LEASE_ACQUIRE_RETRY_SECONDS:-10}
      AppRuntime__EnableTelegramWebhookIngress: ${APP_RUNTIME_ENABLE_TELEGRAM_WEBHOOK_INGRESS:-}
      AppRuntime__EnableTelegramPollingWorker: ${APP_RUNTIME_ENABLE_TELEGRAM_POLLING_WORKER:-}
      AppRuntime__EnableTelegramUpdateQueueWorker: ${APP_RUNTIME_ENABLE_TELEGRAM_UPDATE_QUEUE_WORKER:-}
      AppRuntime__EnableOfficialDataSyncWorker: ${APP_RUNTIME_ENABLE_OFFICIAL_DATA_SYNC_WORKER:-}
      AppRuntime__EnableNotificationWorker: ${APP_RUNTIME_ENABLE_NOTIFICATION_WORKER:-}
      PushNotifications__EnableGameStartPush: ${ENABLE_GAME_START_PUSH:-true}
      PushNotifications__EnableLiveScorePush: ${ENABLE_LIVE_SCORE_PUSH:-true}
      PushNotifications__GameStartLeadMinutes: ${GAME_START_LEAD_MINUTES:-30}
    ports:
      - "${APP_PORT:-8080}:8080"
    volumes:
      - app_data:/app/App_Data
    restart: unless-stopped

  db:
    image: postgres:17-alpine
    container_name: cpbl-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

volumes:
  app_data:
  postgres_data:
